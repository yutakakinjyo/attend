<h1>Courses#index</h1>
<p>Find me in app/views/courses/index.html.erb</p>

<% @courses.each do |course| %>
  <%= course.name %> <%= link_to 'Show', course %> <%= link_to 'Delete', course, method: :delete, data: { confirm: 'Do you want to delete ?' } %><br>
<% end %>
